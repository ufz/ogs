cppcheck:
  stage: check
  needs: [meta]
  tags: [envinf, shell]
  before_script:
    - mkdir -p build
    - cd build
  script:
    - >
      cmake .. -G Ninja -DCMAKE_BUILD_TYPE=Release -DOGS_BUILD_UTILS=OFF
      -DOGS_USE_UNITY_BUILDS=OFF
    - bash cppcheck.sh
  artifacts:
    when: always
    reports:
      codequality: build/cppcheck.json
    paths:
      - build/cppcheck.json

ruff:
  stage: check
  needs: []
  tags: [envinf, shell]
  script:
    - uvx ruff==0.13.1 check --exit-zero --output-format gitlab . > ruff-report.json
    - uvx ruff==0.13.1 check .
  artifacts:
    reports:
      codequality: ruff-report.json
