build linux conan:
  extends:
    - .template-build-linux
    - .test-artifacts
    - .rules-master-manual
  tags: [shell, envinf1-shell]
  needs: [meta]
  variables:
    CMAKE_PRESET: release
    CMAKE_ARGS: >-
      -DBUILD_SHARED_LIBS=ON
      -DOGS_USE_CONAN=AUTO
      -DOGS_USE_MFRONT=ON
      -DOGS_USE_XDMF=ON

build linux native:
  extends:
    - .template-build-linux
    - .test-artifacts
  tags: [shell, envinf]
  needs: [meta]
  variables:
    CMAKE_PRESET: release
    CMAKE_ARGS: >-
      -DBUILD_SHARED_LIBS=ON
      -DOGS_USE_CONAN=OFF
      -DOGS_USE_POETRY=OFF
      -DOGS_USE_MFRONT=ON
      -DOGS_USE_XDMF=ON
